<div>
    <div>{{name}}</div>
    <div>
        <span class="label label-{{#if empty}}success{{else}}important{{/if}}">
            状态:{{#if empty}}空闲{{else}}已用{{/if}}
        </span>
    </div>
    <div class='btn-wrap'>
        <a href="bill?tableId={{id}}" class="btn btn-primary btn-mini btn-bill {{#unless hasOrder}}disabled{{/unless}}">查看</a>
        <span class="btn btn-primary btn-mini btn-change {{#if empty}}disabled{{/if}}">换桌</span>
    </div>
</div>
